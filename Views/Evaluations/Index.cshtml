@model IEnumerable<Airbnb_PWEB.Models.MyEvaluationsViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Lista de Comentários</h1>


<table class="table">
    <thead>
        <tr>
            <th>
                Propriedade
            </th>
            <th>
                Check-In
            </th>
            <th>
                Check-Out
            </th>
            <th>
                Classificação
            </th>
            <th>
                Comentário
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Property.Tittle)
            </td>
           @* <td>
                @Html.DisplayFor(modelItem => item.Reserve.CheckIn)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Reserve.CheckOut)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.EvaluationComment.ReservationId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EvaluationComment.Classification)
            </td>
             <td>
                @Html.DisplayFor(modelItem => item.EvaluationComment.Comment)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EvaluationComment.EvaluationId">Editar</a> |
                <a asp-action="Delete" asp-route-id="@item.EvaluationComment.EvaluationId">Apagar</a>
            </td>
        </tr>
}
    </tbody>
</table>
